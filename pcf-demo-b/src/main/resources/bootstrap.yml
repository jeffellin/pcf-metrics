eureka:
  instance:
    metadata-map:
      zone: zone1
  client:
    region: reg1
    availability-zones:
      reg2: zone3,zone4
      reg1: zone1,zone2
    prefer-same-zone-eureka: true
      # serviceUrl:
    #defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: hop2
app:
  zone: zone1