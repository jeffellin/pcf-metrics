management:
  metrics:
    web:
      server:
        auto-time-requests: true
    distribution:
       percentiles-histogram:
           http.client.requests: true
           http.server.requests: true
  endpoints:
    web:
      exposure:
        include: "metrics,prometheus,env"
  endpoint:
    metrics:
      enabled: true
    prometheus:
      enabled: true
    env:
      enabled: true
spring:
  sleuth:
    web:
      additional-skip: "/actuator/prometheus"
    sampler:
      probability: 1
wavefront:
  application:
    name: jellin-pal
    service: hop1
hop2:
  url: http://localhost:9999/hop
